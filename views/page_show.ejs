<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<meta charset="UTF-8">
	<title>Content</title>
	<script src="lib/marked.js"></script>
</head>
<% include head %>
<div class="content">
	<h1><%= page.title %></h1>
	<%- p_body %>
	<% if (sections != undefined) { for(var i = 0; i < sections.length; i++) { %>
		<h3><%= sections[i].subtitle %></h3>
		<a href="/page/<%= page.p_id %>/section/<%= sections[i].id %>/edit">edit</a><br><br>
      		<%- sections[i].sub_body %>
	<% }} %>
	<br><br>
	<a href="/page/<%= page.p_id %>/sections/new">Add Another Section</a>
</div>
</body>
</html>