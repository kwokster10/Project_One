<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<meta charset="UTF-8">
	<title>Content</title>
</head>
<% include head %>
<div class="content">
	<h1><%= pages.title %></h1>
	<p><%= pages.body %></p>
	<% for(var i = 0; i < sections.length; i++) { %>
		<h3><%= sections[i].subtitle %></h3>
		<p><%= sections[i].sub_body %></p>
	<% } %>
</div>
</body>
</html>