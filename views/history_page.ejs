<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<meta charset="UTF-8">
	<title>History</title>
</head>

<% include head %>

<div class="content">
	<h1><%= history.updated_on || history.created_on %> History for <%= history.p_title %></h1>
	<a href="/page/<%= history.p_id %>/history">Back to <%= history.p_title %> History</a>
	<br>
	<%- body %>
	<br>
	<small><%= history.updated_on || history.created_on %> by <a href="author/<%= history.a_id %>"><%= history.a_name %></small>
</div>
</body>
</html>