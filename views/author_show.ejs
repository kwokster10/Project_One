<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<meta charset="UTF-8">
	<title>Main</title>
</head>
<% include head %>
<div class="content">
	<h1><%= author.name %></h1>
	<a href="/author/<%= author.id %>/edit">edit</a><br>
	<%- bio %>
	<h3>Authored:</h3>
	<ul>
	<% if (pages != undefined) { for(var i = 0; i < pages.length; i++) { %>
	<% if (pages[i].created_on != null) { %>
		<li><a href="/page/<%= pages[i].p_id %>/history"><%= pages[i].p_title %></a></li>
	<% }}} %>
	</ul>
	<br>
	<h3>Contributed To:</h3>
	<ul>
	<% if (pages != undefined) { for(var i = 0; i < pages.length; i++) { %> 
	<% if (pages[i].updated_on != null) { %>
		<li><a href="/page/<%= pages[i].p_id %>/history"><%= pages[i].p_title %></a></li>
	<% }}} %>
	</ul>
	<br>
</div>
</body>
</html>