<!DOCTYPE html>
<html lang="en">
<head>
	<link rel="stylesheet" type="text/css" href="/style.css">
	<meta charset="UTF-8">
	<title>New Section</title>
</head>

<% include head %>

<div class="content">
	<h1>Adding a Section to <%= page.title %></h1>
	<p>
	<form action="/page/<%= page.id %>" method="POST">
		<label>Who are you?</label>
		<select name="author_name">
		<% for (var i =0; i < authors.length; i++) { %>
			<option value="<%= authors[i].name %>"><%= authors[i].name %></option>
			<% } %>
		</select><br><br>
		<input type="text" name="subtitle" placeholder="section title" required><br>
		<input type="hidden" name="page_title" value="<%= page.title %>">
		<br>
		Information relating to section title:<br>-Fill this out in markdown format-<br>
		<textarea name="sub_body" rows=20 cols=60 required></textarea>
		<button>Submit</button>
	</form>
	<br>
	*If you are not sure what markdown format is, check out the 'How To Edit' page listed on the left. 
	</p>
</div>

</body>
</html>


